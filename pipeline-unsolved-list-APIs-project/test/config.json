{
    "rabbitMQ": {
        "hostName": "localhost",
        "queueName": "test_queue",
        "exchange": "test_exchange"
    },
    "database": {
        "hostName": "localhost",
        "port": 3306,
        "schemaName": "test_db",
        "userName": "test_user",
        "password": "password"
    },
    "transform": {
        "user_filtering_key": ["username", "id", "email"],
        "user_table": "site_users",
        "query": "WHERE username = forever",
        "problem_filtering_key": ["life"],
        "user_columns": ["username", "id", "email"]
    },
    "api": {
        "ORG_API_URL": "https://example.com/org",
        "USER_API_URL": "https://example.com/user",
        "TARGET": "lists",
        "COUNT": "total_count"
    }
}